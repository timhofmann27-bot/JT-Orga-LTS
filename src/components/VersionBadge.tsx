import React from 'react';

const VersionBadge = () => {
  return (
    <div className="absolute top-4 left-4 text-xs font-semibold text-white/60 bg-black/50 px-2 py-1 rounded">
      v1.2
    </div>
  );
};

export default VersionBadge;